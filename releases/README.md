# Model releases

This directory is reserved for manually promoted Hansa model releases. Curriculum training does
not create, rotate, overwrite, or delete files here. Automatic rolling recovery snapshots live
under `training_output/backups/` instead.
